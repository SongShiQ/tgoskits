args = [
    "-nographic",
    "-m", "1G",
    "-cpu", "cortex-a53",
    "-device", "virtio-blk-pci,drive=disk0",
    "-drive", "id=disk0,if=none,format=raw,file=${workspace}/tmp/axbuild/rootfs/rootfs-aarch64-debian-llama.img",
    "-device", "virtio-net-pci,netdev=net0",
    "-netdev", "user,id=net0",
    "-append", "root=/dev/sda",
]
uefi = false
to_bin = true
shell_prefix = "root@starry:"
shell_init_cmd = "/usr/bin/llama-test.sh"
success_regex = ["LLAMA_DEBIAN_TEST_DONE"]
fail_regex = [
    "(?i)not found",
    "(?i)segmentation fault",
    "(?i)kernel panic",
    "(?i)panicked at",
    "ENOSYS",
    "Aborted",
]
timeout = 600
